# Attach These Files

Send plan: one email, attachments organized as two folders -

- **Folder 1 "DOS and Seebeck (SrCu2SnS4)"** = the four files in
  `../second step result (DOS and Seebeck)/READY_TO_ATTACH/`.
- **Folder 2 "Three-material first pass"** = the eleven files below, all
  collected in this package's `READY_TO_ATTACH/`.

Folder 2 contents:

1. `three_material_comparison.md` - readable side-by-side comparison
2. `three_material_comparison.csv` - same table, machine-readable
3. `three_material_transport_comparison.png` - two-panel figure: best sampled
   PF/tau vs T (per tau, log scale) and electronic-only zT_e at those points
4. `three_material_transport_best_power_factor.csv` - best-PF/tau point per
   material, temperature, and carrier sign (14 rows each)
5. `SrZrS3_Seebeck_vs_mu.png` - Seebeck centered on E_F, same format as the
   SrCu2SnS4 figure in Folder 1
6. `SrZrS3_Seebeck_vs_mu.csv`
7. `Rb2Cu2SnS4_Seebeck_vs_mu.png`
8. `Rb2Cu2SnS4_Seebeck_vs_mu.csv`
9. `SrCu2SnS4.relaxed.cif`
10. `SrZrS3.relaxed.cif`
11. `Rb2Cu2SnS4.relaxed.cif`

Do not attach raw QE logs, `.bt2` files, `.dos` files, charge density,
wavefunctions, or temporary folders. They are large and not needed for a
progress update; the full records stay in the material workspace.

The email was sent with both folders (as folder1.zip / folder2.zip,
byte-verified against the two `READY_TO_ATTACH/` folders). Both packages'
`READY_TO_ATTACH/` folders are now **frozen** records of what was sent.
