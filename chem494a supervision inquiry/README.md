# CHEM 494A supervision inquiry

The email asking Professor Kleinke to supervise the CHEM 494A research
project in the upcoming Winter term, opening with a brief summary of the
work done with Roy so far. This is a communication package, not a numbered
step-result folder: it contains no new calculations.

- `EMAIL_DRAFT.md` — the draft to send. Send it from the university
  account (the one used for all group correspondence and the CCDB
  registration). The recipient's address is deliberately not stored in
  this public repo; take it from prior correspondence or the department
  directory.
- `WORKLOG.md` — dated, append-only history of how the draft was prepared.

Status: DRAFT — not yet sent. Record the outcome in `WORKLOG.md` after
sending.
