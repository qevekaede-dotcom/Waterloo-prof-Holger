# Phonon research sandbox work log

## 2026-09-21 — Isolated baseline and finite work plan

- Inspected original cwd, Git status, remotes and all existing worktrees. Original and both existing worktree tracked states were clean. No other worktree was edited.
- Fetched origin and confirmed `codex/complete-three-material-phonons` at `cf21cce05c7107120a52ac2658f8dbe05bc0a864`. Experiment branch did not exist locally or remotely. Created isolated worktree and `codex/phonon-research-sandbox` from that exact remote tip.
- Read continuity/scientific rules and latest handoff. Older memory describing no Rb FC files is superseded by repository evidence from September 20.
- Launched three disjoint native agent cards: Sol scientific plans, Sol tooling, Terra dashboard/analysis. A fresh Sol reviewer follows integration. No remote-compute action is authorized in this round.
- Identified contradictory root CLAUDE/README_START_HERE/Roy task status statements and a stale README main-baseline instruction. Archived the previous HANDOFF verbatim before making a concise current-state entry.
- Finite deliverables: three execution-ready proposals; demonstrated software fixes and focused tests; offline Chinese evidence dashboard and Rb analysis; status reconciliation; file-anchored guide and reporting PPT; independent review; staged Git commits/pushes.

## 2026-09-21 — Implementation and integration

- Sol tooling worker repaired stdout fatal-signature detection, bound v2 pristine input bytes before health writes, and included the dataset semantic validator digest in provenance. No physical thresholds or models changed; historical run scripts remain frozen. Worker ran campaign suite (435 passed), v2 suite (18 passed), and focused parser/historical evidence checks (13 passed).
- Sol scientific worker wrote three material plans and targeted primary/official source references. Coordinator corrections removed causal overstatement about SrCu cutoff dominance, prohibited cross-supercell force reuse, distinguished proposed transplanted thresholds from implemented Rb gates, and separated a small timing subset from a full amplitude matrix.
- Terra dashboard worker extracted 105 Rb rows (5 grids × 3 temperatures × 7 quantities), reproduced all 12 archived transition rows, and created an offline HTML view/SVG. Coordinator found mobile overflow and a misleading grid-matrix label and requested a bounded UI/documentation correction.
- Coordinator extracted electronic PF/tau and Rb chart data with source hashes, authored an 11-slide English PPTX with bilingual companion script, and wrote a file-anchored QE-to-kappa learning guide. User explicitly requested all-English PPT content; XML verification found no Chinese in slide, chart or note parts.
- Presentation build attempts exposed integration issues: missing native-table policy flags, excessive raw numeric precision for Excel snapshots, default chart-label fonts, and a missing runtime module environment variable. Fixed the builder/configuration; did not weaken validation or change scientific source data. Finalization passed package integrity, layout/font, editable charts/workbooks and first-party reimport checks. Native PowerPoint UI testing is not claimed.
- Playwright's bundled browser was absent. Used installed Chrome in a temporary headless context instead of downloading a browser or using a personal browser session. Desktop rendered without JavaScript errors, with 12 transition rows and 105 tensor rows. Mobile layout repair is being integrated.
- No current cluster access, job submission, heavy physics calculation, outbound message or modification of original evidence took place.

## 2026-09-21 — Independent review and bounded corrections

Fresh Sol reviewer returned `fix-first` with three actionable findings:
(1) SrZr q=(0,2/5,0) needs a commensurate frozen-mode supercell;
(2) v2 input hashes and supplied healthy output do not establish execution provenance;
(3) one physical comparison cannot satisfy a two-transition convergence rule.

Resolution keeps scope local and preparation-only. The v2 reports now explicitly
publish `execution_provenance_verified=false` and scoped health/preflight
semantics. A future externally anchored launch/exit receipt protocol is documented,
not implemented. This is an unresolved pre-execution dependency, not a fake
provenance pass. The existing v2 suite passed all 18 tests after these additions.
Scientific plans are being aligned to q-commensurate mode construction and
conditional three-level physical studies. The original review findings stay in
the review record; a narrow disposition check follows these specific corrections.

The final dashboard was exercised in headless Chrome: no JavaScript errors,
12 transition rows, 105 tensor rows, body widths 1440 at a 1440 viewport and
390 at a 390 viewport. A long footer code path was the remaining overflow and
was fixed with text wrapping. All final PPT slides were rendered from the exact
final package and inspected; a shareable Rb slide preview is included.

## 2026-09-21 — Review closure and delivery checkpoint

- SrZr plan now derives frozen-mode periodicity from q·T integer after basis
  and eigenvector mapping verification; harmonic interpolation cells remain
  distinct from a commensurate distortion cell.
- SrCu/Rb plans now explicitly sequence baseline, next candidate, and a
  conditional third level when applying the proposed two-transition physical
  convergence rule. One comparison remains provisional pilot evidence.
- Narrow Sol disposition accepted those fixes and the explicit v2 provenance
  limitation, conditional on one stale error message correction. Corrected the
  exact message to separate supplied-log health and audited input-byte checks;
  no logic changed. The review's local-scope ship condition is fulfilled.
- Final deliverables: three plans with sources, offline Chinese evidence
  dashboard, 105-row Rb data and SVG, demonstrated source fixes, file-anchored
  guide, 11-slide all-English PPT with editable charts/tables and bilingual
  script. No new accepted kappa or full zT claim.

## 2026-09-21 — Final packaging cleanup

The staged whitespace check detected default CSV CRLF line endings in the newly
derived Rb table. Normalized only this new CSV writer to LF, regenerated the
CSV/data hashes/embedded page, and verified all 105 parsed rows are identical.
Original historical CSV files were not changed. Final navigation inspection also
found the sandbox README had become dashboard-only during integration; restored
the full deliverables index and kept the useful rebuild explanation in analysis/README.md.
These packaging corrections do not change research values or scientific standards.

## 2026-09-22 — Second bounded local-development round

User asked to continue useful work with remaining allowance. Re-read current
instructions/handoffs, inspected all worktrees and fetched origin. Sandbox
`dfefd48` matches its remote; original worktrees were clean. Started three
independent cards: execution-receipt prototype (Sol high), exact q/supercell
commensurability helper (Sol high), and Rb saved-data reuse inventory (Terra
medium). Deterministic route recommendations are saved under round2/. Coordinator
owns integration, all-temperature component diagnostics and bilingual report Q&A.
All new work is local and lightweight. No scheduler query, job operation, real QE,
FC fitting, kappa solve, email or scientific-threshold change is in this round.

## 2026-09-22 — Component diagnostics and reporting preparation

Recomputed 48 Rb diagonal/trace changes directly from the saved audit tensors,
cross-checked all values and 12 joint verdicts against the saved audit, and made
an all-temperature figure. All 12 zz checks fail, while six trace-only checks
pass. The documentation distinguishes cancellation (45→60) from dilution
(75→90); neither establishes tensor or physical convergence. Added 12 bilingual
practice questions with evidence links; the existing English PPT is unchanged.
The first plotting attempt used the bundled document Python, which lacks
matplotlib. Used the existing thermo-bt2 environment (matplotlib 3.11.0, numpy
2.4.6); no package installation or heavy scientific calculation was needed.
PNG visual inspection passed. Added the new tools and figure to the offline
dashboard; final link/browser checks follow once worker files are complete.
